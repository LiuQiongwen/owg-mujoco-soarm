# Round 1 Review (Self-Review — Codex/GPT-5.4 external review failed with 401 Unauthorized, no valid API credentials configured in this environment)

Reviewer note to user: this is my own critical pass, not GPT-5.4's. I've tried to be genuinely adversarial rather than rubber-stamp my own round-0 proposal, and I backed it with new evidence (fetched the actual C²OT source code, not just its abstract) rather than pure argument. Treat the scores as indicative, not authoritative — there is no substitute for a real external reviewer here.

## Scores (1-10)

1. **Problem Fidelity: 7/10.** The success condition is still anchored to "restore/exceed the baseline the original paper needed," which is right. But there's a subtler fidelity issue: the *original* paper's bottleneck was "build a working grasp-candidate generator." The revised bottleneck is "diagnose and fix a training-time OT-coupling defect in flow matching." These are related but not identical — the second is now substantially a *generative-modeling methodology* contribution wearing a robotics-application costume. That's not necessarily wrong, but it's a real reframing, not a pure continuation, and should be named as such rather than presented as a seamless continuation of the original anchor.

2. **Method Specificity: 5/10 in round 0, now correctable.** Round 0 *guessed* the C²OT cost formula (`cost(i,j) = ||x0_i-x1_j||² + λ·1[c_i≠c_j]·penalty`) without having read the actual method. I have now fetched `c2ot/ot.py` directly from the released repo. The **real** mechanism is materially different from the guess in three ways that matter for implementation:
   - The condition cost `C` is a **pairwise distance between condition embedding vectors** (cosine or L2², continuous-valued), not a binary same/different indicator.
   - The final assignment is solved via **exact `scipy.optimize.linear_sum_assignment`** (Hungarian algorithm), not Sinkhorn/entropic OT as round 0 vaguely hedged.
   - Critically, for their **CIFAR-10 config (10 discrete classes — the closest existing analogue to our 7 discrete YCB objects)**, they do not use the adaptive `target_r`-search at all (`target_r: null`); they hardcode `cls_weight (condition_weight) = 1e8`, `condition_norm: l2_squared`, plus `oversample_ratio: 5` (OT solved over a batch 5x larger than the network minibatch). A weight of 1e8 on a same/different-class cost is not a "gentle nudge" hyperparameter — it is effectively a **hard constraint that forces within-class pairing whenever possible**. **Priority: CRITICAL — fixed in the revision below.**

3. **Contribution Quality: 6/10.** One dominant + one supporting contribution is the right shape, and Claim 2 (consensus selection) is genuinely already evidenced and cheap to include. But the dominant contribution's novelty claim ("first to show this on physical task success, not just FID") is a *transfer* novelty, not a *mechanism* novelty — worth being honest in the paper that the mechanism itself is entirely borrowed (verbatim, from released code), and the contribution is the transfer + empirical validation, not a new algorithm. This is fine for RA-L (application venues reward exactly this kind of transfer), but round 0's prose oversells it slightly as if the coupling formula itself were new. **Priority: IMPORTANT — reword novelty framing to be explicit about "borrowed mechanism, new application + physical validation," not "we designed a fix."**

4. **Frontier Leverage: 7/10.** Correctly resists bolting on an LLM/VLM for its own sake. But there is an unexamined, *simpler* frontier-appropriate alternative that round 0 dismissed too quickly: given that a discrete, 7-class, per-object OT coupling with a near-infinite condition weight is mathematically equivalent (in the limit) to **just running standard OT coupling independently within each object's own training pairs** (i.e., stratify the minibatch by object before computing OT, rather than computing one global assignment and penalizing cross-object pairs to death) — this is a *simpler-to-implement*, exactly-equivalent-in-effect special case for the discrete/small-class regime we're actually in, and it requires no new cost-function code at all, just a for-loop over object labels around the *existing* unconditional OT call already in `train_cfm_grasp.py`. Round 0 should have proposed this directly rather than the generic weighted-penalty formulation, which is unnecessary generality for a 7-class discrete problem. **Priority: IMPORTANT — this is a real simplification, not just style.**

5. **Feasibility: 6/10.** The compute/timeline estimate assumes: (a) the retrain is a drop-in change, now more true with the per-object-stratified-OT simplification; (b) 1-2 days for a full clean 4-way n=50/7-object re-evaluation is achievable — this is optimistic given the actual wall-clock pace observed *this session* for smaller batches (a 300-trial batch took a meaningful fraction of an hour to multiple hours in practice); (c) **the deadline itself is unstated** — round 0 says "tight" without a date. This is a real gap: whether this whole pivot is even attemptable depends entirely on days-remaining, and I should not keep planning a multi-day new campaign without confirming that. **Priority: CRITICAL — must ask the user for the actual submission deadline before committing further compute.**

6. **Validation Focus: 8/10.** The 4-way comparison plus the λ→∞ (or now, "stratify vs. don't stratify") internal ablation is tight and claim-driven. No bloat.

7. **Venue Readiness: 6/10.** RA-L reviewers are typically robotics/controls-oriented, not generative-modeling theorists. A paper whose central technical move is "we found a very recent (4-month-old) generative-modeling paper's failure mode and confirmed it transfers to our robot" is a good *application/validation* story for RA-L, but round 0's framing leans quite heavily into flow-matching-coupling mechanism language in a way that may read as out-of-scope for reviewers expecting a robotics contribution (a task-level metric or manipulation-specific insight). Consider foregrounding the physical/task-level framing ("a previously-hidden training defect silently made a published-style robot-learning claim, and reversing it required going back to a very recent ML-methodology paper") more than the OT-cost-matrix math itself, which can live in a compact paragraph + equation, not dominate the narrative. **Priority: IMPORTANT.**

**OVERALL SCORE (weighted): 6.4/10**

## Simplification Opportunities
1. Replace the generic condition-weighted-cost-matrix framing with the **discrete special case**: stratify each training minibatch by object label and run the *existing, already-implemented* unconditional OT coupling independently within each object's ~400 examples, instead of across the mixed 7-object batch. This is mathematically what C²OT's own CIFAR-10 config (`cls_weight=1e8`) converges to for discrete classes, is far simpler to implement (a groupby, not a new cost function), and removes the need for any new hyperparameter search (no λ sweep needed at all — there is no λ once you stratify).
2. Given (1), the "λ=0 ablation" in round 0 collapses into simply "the existing, already-collected condition-agnostic OT-CFM run" — no new ablation needs to be run, saving compute.

## Modernization Opportunities
NONE beyond what's already appropriately conservative — the SAM-embedding conditioning signal is unchanged, and no LLM/VLM addition is warranted by the diagnosed bottleneck.

## Drift Warning
Partial. The paper's contribution has shifted from "a new generative-candidate method for grasping" to "an empirical transfer-and-validation study of a recent generative-modeling failure mode, in a robotics setting." This is a legitimate, defensible RA-L contribution, but it is a different *kind* of contribution than originally anchored, and the proposal should say so explicitly rather than paper over it.

## Verdict: REVISE

Two CRITICAL items block READY: (1) the method must use the verified, discrete-class-appropriate mechanism (stratified OT / hard condition weight), not the generic guessed formula; (2) the actual submission deadline must be confirmed before committing to another multi-day experimental campaign, given how much compute this session has already spent.
