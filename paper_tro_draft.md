# Causal-Validity Auditing for Learned Grasp-Candidate Scoring, with a Case Study in Cross-Embodiment Grasping

**[T-RO Submission Draft]**

---

## Abstract

A learned scorer that reranks grasp candidates before execution is valid only if every input feature is computable *before* the chosen candidate is executed — a constraint we call causal validity. It is easy to violate silently, because an offline accuracy evaluation looks identical whether a feature is causally valid or not: historical logs already contain the outcome of every candidate they describe. We formalize a pre-execution admissibility criterion, build a reference audit tool, and show its practical necessity on our own cross-embodiment grasp-reranking pipeline (a 5-DoF SO-ARM101 arm to a 6-DoF Piper arm, in simulation): a pooling effect that replicated five times at p<0.0001 collapses to an exact null (p=1.0000) once causally invalid features are removed. We then automate the audit with a static dataflow analyzer that infers feature provenance from source code rather than manual registration, and show it independently catches a contamination bug our own hand-built audit had missed and had already reported as clean — with a measurable downstream consequence on a previously reported result. Another, independently discovered infrastructure bug (an unseeded placement sampler, invalidating an implicit pairing assumption) let us re-verify our core finding under corrected data and stress-test a newly available admissible feature that appeared, in a naive pooled analysis, to be the strongest predictor in the entire investigation (r=−0.55) — only for a within/between-scene decomposition to reveal zero real selection-relevant signal, a sharper demonstration of this paper's thesis than the finding it was built to test. We validate the criterion externally against two recent published systems, report a further, independently discovered bug (a silent unit-scaling error in a widely used composite robot controller) as a case study in the same discipline, and report eleven independently tested and ruled-out mechanisms for a still-open execution-time reliability problem, each evaluated with a paired statistical test. We close with a hardware deployment architecture and safety checklist, explicitly scoped as not yet physically validated.

---

## I. Introduction

Grasp-candidate reranking — training a lightweight scorer to select the most promising pose from a pool of candidates before executing any of them — is an established pattern in robotic manipulation [Dex-Net 2.0; GraspGen]. Its correctness rests on an assumption rarely stated explicitly: every feature the scorer consumes must be computable *before* the chosen candidate is executed. A feature that depends on execution's outcome — contact force during approach, positional drift during descent, a kinematic residual re-solved after the arm has already moved — cannot be known at the moment a not-yet-executed candidate must be scored, however predictive it looks offline.

This constraint is easy to satisfy by construction and easy to violate by accident. GraspGen [arXiv:2507.13097] satisfies it by construction: its discriminator takes only a point-cloud encoding and a candidate pose at inference time, while training labels come from simulated physics outcomes. It is violated by accident whenever a feature logged for training-data purposes gets reused, unexamined, as a live-selection input — and the violation is invisible under the evaluation method such systems reach for first: offline accuracy on historical logs, which already contain every candidate's outcome and so cannot distinguish a valid feature from an invalid one.

We hit this failure mode directly while extending a validated single-embodiment grasp reranker (LGGSN [RA-L, under review], pairwise-BPR-trained on a 5-DoF SO-ARM101 arm) to a second, independently implemented 6-DoF platform (a Piper arm, MuJoCo/RoboSuite simulation). A cross-embodiment pooling effect replicated across five independent pilots at p<0.0001, effect size growing from +0.21 to +0.35 AUC as feature sets were enriched. Designing the live-execution test this result was building toward required, for the first time, identifying which features would actually be available to a not-yet-executed candidate. None of the features driving the five-times-replicated result qualified. Restricting to the causally valid subset collapsed the effect to an exact null (p=1.0000).

This paper is organized around that discovery, not around a single proposed algorithm for improving grasp success. Contributions:

1. A formal **pre-execution admissibility criterion** (§IV), including a subtlety required in practice: a feature may depend on sibling, not-yet-executed pool candidates without being causally invalid — conflating "depends on multiple candidates" with "depends on execution" is itself a documented failure mode of our own reference implementation.
2. A **reference audit tool**, enforced as an automatic training-time gate, externally validated against two recent published systems (§IV-A–B).
3. An **automated dataflow analyzer** inferring feature provenance from source code rather than manual annotation, which independently caught a contamination bug our manual audit missed and had reported as clean — with a measurable downstream consequence (§IV-D).
4. A **second, independent case study** in the same discipline: a silent double-scaling bug in a widely used composite robot controller, validated on three previously near-zero-success object categories (§V).
5. An **honest, statistically rigorous ledger of eleven ruled-out mechanisms** for the execution-time reliability problem our diagnostics converged on, none presented as solved (§VII).
6. A **hardware deployment architecture and safety checklist**, scoped plainly as not yet physically validated (§VI).

We do not claim to have solved cross-embodiment grasp transfer or the execution-precision problem our diagnostics repeatedly point at. We claim that trying to solve them surfaced a general, underappreciated evaluation failure mode with a formal characterization and a checkable fix — and that even a careful team applying that fix to its own work will still find real instances of it, which is worth reporting rather than hiding.

---

## II. Related Work

**Grasp candidate scoring.** Dex-Net 2.0 [RSS 2017] trains a grasp-quality CNN offline on millions of simulated grasp outcomes, then deploys the trained network — not a live simulation query — to rank real candidates from a depth image at inference time; execution-derived information appears only as training supervision. GraspGen [arXiv:2507.13097] follows the same pattern with an on-generator-trained discriminator whose inference-time inputs are a point-cloud encoding and a candidate SE(3) pose. Neither work states the input/label distinction as a formal, checkable criterion; both satisfy it, which we use as external validation of our formalization (§IV-A) rather than as prior art for the criterion itself.

**Path-aware grasp feasibility.** Recent work on grasp-dependent feasibility prediction [arXiv:2512.18922] generates causally-admissible, path-level geometric labels — inverse-kinematics feasibility and a transit-collision flag from mesh sweeps along a fixed waypoint template — and trains a compact MLP on them for candidate ranking, validated as transferring to physics-executed trajectories on a single rigid cuboid with a fixed waypoint template. This is framed entirely as an efficiency argument (cheap labels vs. expensive physics simulation), with no connection to a causal-validity theory. We considered adapting this mechanism for our own still-open execution-reliability problem and rejected it (§VII) on the grounds that our diagnosed failure mode (sustained, necessary contact under sub-millimeter miscentering) does not match the binary avoidable-collision framing this mechanism targets, and is mechanistically closer to four contact-response interventions we had already tested and found ineffective.

**Cross-embodiment transfer in grasping.** GraspGen-X [arXiv:2606.00998] demonstrates cross-embodiment 6-DoF diffusion-based grasping validated on a real AgileX Piper arm, using pointwise (not pairwise) supervision. Freeform Preference Learning [arXiv:2606.32027] learns a language-conditioned reward model from trajectory-level human preferences for post-hoc reward shaping in reinforcement learning — a role that legitimately requires execution-derived input by design, which we use as a boundary case our criterion correctly does not flag (§IV-B).

**Data leakage and offline evaluation.** Look-ahead and label leakage are recognized general problems in machine learning and have been studied recently in offline reinforcement-learning policy evaluation [arXiv:2605.11479], where an oracle policy may exploit information unavailable to the deployed agent. Closest to our formal criterion is concurrent work by Roth [arXiv:2603.10742], a grammar of eight typed primitives with a call-time-enforced admissibility gate that makes train/test leakage structurally unrepresentable in standard tabular and time-series ML pipelines. Two differences: domain (Roth's boundary is the train/test split; ours is whether a candidate has physically executed yet, including a pool-relative case with no analogue in a train/test framework) and mechanism (Roth's grammar requires authoring a pipeline against his primitives; our tool statically analyzes arbitrary, already-written code via a single per-function marker, no pipeline rewrite required). We are not aware of prior work stating our specific criterion for grasp-candidate scoring features, nor of an automated tool for detecting the violation from unmodified source code in this domain.

**Perception uncertainty for grasping.** Closest to our planned real-noise-characterization extension (§VIII) is Consensus-Driven Uncertainty [Joyce et al., IROS 2025, arXiv:2506.20045], which trains an MLP to predict grasp failure from disagreement among three independent pretrained pose estimators (EPOS, GDRNPP, ZebraPose) applied to the same real image, and reports that "poses perturbed by Gaussian noise will not fail the same way that real pose estimators fail" — independent evidence for the same hypothesis motivating our own plan. Two gaps remain open relative to our design: that work does not quantitatively characterize the noise's statistical structure (per-axis variance, Gaussianity, position-orientation correlation), and its uncertainty signal drives an accept/reject gate on a single proposed grasp rather than a selection rule over a candidate pool.

**Contact-aware execution.** CoorGrasp and related work on wrench-balance-criterion phase transitions use real tactile sensing to coordinate arm motion after fingers have already formed contact with an object. Lacking tactile hardware, we used MuJoCo's own contact-force API as a privileged, hardware-free proxy for this class of mechanism (§VII), finding it — along with three related contact-response variants — ineffective for our specific failure mode.

---

## III. System

Our platform is a 6-DoF AgileX Piper arm simulated in MuJoCo via RoboSuite (`tango_robot/piper_robosuite/`), a second, independently implemented embodiment alongside this project's original 5-DoF SO-ARM101 platform. The core execution primitive, `run_pick_and_place`, drives the arm through a fixed phase sequence — transit, approach, descend, close, lift, transit-to-tray, lower, retract — using DLS inverse kinematics solved against a saved kinematic snapshot (`ArmIK.solve_multi_seed`) and an absolute joint-position controller. `PiperMultiObjectScene` provides a multi-object tabletop scene with a placement tray; success is defined as final object position within a fixed radius of the tray center.

LGGSN, this project's validated single-embodiment contribution, is a lightweight MLP-based pairwise reranker trained with Bayesian Personalized Ranking loss over a 14-dimensional geometric feature space on the SO-ARM101 platform, and is the deployed, live candidate scorer for that platform's Stage-4 pipeline (`grasp_ranker_lggsn.py`, invoked from `tango/policy.py`). It is both reused infrastructure in this paper and, in §IV, the system under audit — the causal-validity criterion is applied directly to its live, already-deployed feature pipeline, not to a toy example.

---

## IV. Causal-Validity Auditing for Learned Grasp-Candidate Scorers

### Definitions

**Definition 1 (Candidate pool, execution trace).** A scene induces a candidate pool `C = (c_1,...,c_N)`, each candidate specified by a pose `p_i ∈ SE(3)` and generator-time attributes `a_i`. Let `G` denote static, execution-independent scene/object information. If `c_i` is physically executed, let `T_i` denote its execution trace — contact forces, intermediate poses, net displacement, and the terminal success label `y_i ∈ {0,1}`.

**Definition 2 (Selection-time information set).** At the moment a scorer must choose which candidate(s) to execute, the selection-time information set is `I(C) = {p_1,...,p_N} ∪ {a_1,...,a_N} ∪ G ∪ K(C,G)`, where `K(C,G)` is the set of values obtainable from isolated, execution-free queries against a candidate pose and `G` (e.g. a kinematic IK residual or collision check evaluated without moving the arm). `I(C)` contains no `T_j` for any `j` — nothing in the pool has executed yet.

**Definition 3 (PRE_EXECUTION-admissible feature).** A feature function `φ` is admissible if there exists `g` such that, for every candidate `c_i` in every pool `C`, `φ(c_i) = g(I(C))`. `I(C)` includes every candidate's pose, not just `c_i`'s own — a feature may depend on sibling candidates (e.g. distance to the pool centroid) without being execution-derived. Conflating "depends on more than one candidate" with "depends on execution" is a documented failure mode of our own reference implementation (§IV-C). Otherwise `φ` is EXECUTION_DERIVED.

**Proposition.** A scorer `S(c_i) = h(φ_1(c_i),...,φ_k(c_i))` used to select among not-yet-executed candidates is causally valid iff every `φ_j` is admissible. A feature set containing an execution-derived `φ_j` is invalid regardless of how well `S` performs offline on historical `(c_i,T_i)` pairs — offline evaluation implicitly supplies `T_i` for every evaluated `i`, satisfying `φ_j` even though it would be undefined for a genuinely new candidate.

**Corollary.** Execution-derived quantities remain valid as supervision targets or as values used only to construct them — the constraint binds the input side of `S`, not the label side. This is why GraspGen's design (§II) is correct.

**Algorithm.** (1) Tag every raw field with provenance `τ(f) ∈ {PRE, EXEC}` by tracing the *code path that computes it*, not a comment or an assumption about a similarly named field. (2) Before training or invoking any live-selection scorer with feature set `F`, reject if any `f ∈ F` has `τ(f)=EXEC` or is unregistered — fail closed. (3) Re-run (2) whenever `F` changes *or whenever the code computing any `f ∈ F` changes*, since `τ(f)` is a property of code, not of a field's name.

### IV-A. External grounding

We checked the criterion against two recent published systems chosen to stress-test both directions of the boundary. GraspGen [arXiv:2507.13097] is confirmed to satisfy it: its discriminator's inference-time inputs are a point-cloud encoding and candidate pose only; training labels are simulated shaking-motion outcomes. Freeform Preference Learning [arXiv:2606.32027] is confirmed as a correct *non-flagged* boundary case: its reward model legitimately takes trajectory-level, execution-derived input, because its role in the pipeline — post-hoc reward shaping for reinforcement learning — is categorically different from live pre-execution candidate selection. The criterion correctly distinguishes "execution-derived input because the model's role requires it" from "execution-derived input leaked into a model whose role requires admissibility," which is the failure mode we hit.

### IV-B. Retrospective demonstration

We implemented the registry (`FieldSpec`/`audit_feature_set()`) covering every raw field logged by both platforms' pipelines and ran it against every feature set used across this project's cross-embodiment reranking pilots, in chronological order. The audit correctly flags every pilot that produced the illusory pooling-beats-zero-shot effect and passes every pilot ultimately trusted; it attributes the contamination specifically to the Piper side (`quality_score`, a descend-phase IK residual logged after physical motion; `correction_proxy`, a positional-drift measurement by definition only available post-motion), while the SO-ARM101 side's superficially similar fields (`score`, `dz`, `dz_lift`, `need_dz`) — traced against the actual live inference code rather than a comment describing an inactive legacy dataset — turned out to be a harmless pre-execution proxy plus dead constant-zero features.

### IV-C. Self-corrections

Building this registry produced two errors of its own, both caught by re-tracing against live code rather than trusting an initial plausible explanation: (1) the SO-ARM101 fields above were initially mislabeled execution-derived by quoting a comment describing a different, unused dataset; (2) two pool-relative features (`dist_to_centroid`, `z_rel`) were initially mislabeled by conflating "depends on sibling not-yet-executed candidates" with "depends on execution." We report both because they demonstrate the discipline the method argues for, applied to itself, not because they weaken the tool's credibility.

### IV-D. Automated tagging

A manually populated registry does not scale and, as §IV-C shows, is itself error-prone. We built `auto_tagger.py`, a static dataflow analyzer that infers provenance automatically: a fixed-point pass over the module identifies every *execution-touching* function (one that directly or transitively calls the simulator's physical-actuation entry point), and a forward taint-propagation pass over the target function's body — gated by a single human-placed marker denoting the point after which the current candidate has begun physically executing — determines, for every field in the function's output, whether it is reachable from any post-marker execution-touching call or live-state read. This reduces the manual annotation burden from *O(number of logged fields)* to *O(number of physical commit points in the codebase)* — a single marker, in our case, rather than thirteen-plus per-field judgments.

Run against the real, live `run_pick_and_place` function, the tagger independently disagreed with our hand-built registry on one field: it flagged `grasp_yaw` as execution-derived, where the manual registry had marked it admissible. Tracing why confirmed the tagger was correct: the pose variable `grasp_yaw` is derived from is reassigned twice — once pre-commit during candidate selection (admissible) and once post-commit, at a deliberate mid-trial drift-correction step that re-reads simulator state after the descend phase has already executed. A human reading the function once sees the first assignment and does not track the second; the automated tagger mechanically follows every reassignment. This was not a cosmetic error: `grasp_yaw` was part of the feature set our own prior retrospective validation (§IV-B, "corrected" configuration) had already reported as clean. Re-running that validation with a genuinely admissible two-feature set (`[z, H]`, dropping `grasp_yaw` from both platforms for a fair shared feature space) confirmed the qualitative finding is unchanged — all pooling conditions remain exactly identical to zero-shot transfer (diff=0.0000) — but the reported absolute accuracy changed from 0.8236 to 0.1327, a number that had already been published as final in this project's own working documentation before the automated check caught it.

We verified, rather than assumed, why: `H` is an exact constant across every row of the single-object (Cracker-only) Piper collection (σ=0 over n=250), and `z` is a near-constant *per scene* — 14/25 scenes show within-scene spread under 2×10⁻⁴, consistent with floating-point/physics-settling noise rather than a genuine per-candidate signal, because it is read once, from the object's own spawn pose, before any candidate-specific action — identical regardless of which of the ten pooled candidates is later attempted. Together, `[z, H]` has almost no capacity to discriminate between different candidates drawn from the *same* scene, which is exactly the comparison the pairwise ranking objective is trained and evaluated on. This explains the exact equality across all four pooling conditions and reframes 0.1327 not as an unexplained oddity but as an expected consequence of a feature set that is causally valid yet structurally near-uninformative for this specific comparison — a genuinely predictive but still-admissible per-candidate feature (e.g. the original, pre-commit candidate orientation, distinct from the execution-derived value §IV-D identified) was not tested here and is left as future work.

We validated the tagger against a second, structurally different function (`_emit_lggsn_candidates`, a pure data-serialization routine with no internal execution boundary and a `dict()`-constructor field pattern rather than a `return` literal) and initially found a genuine scope limitation worth stating rather than hiding: because this function contains no physical-commit marker of its own, every field trivially returns admissible, which happens to be correct (verified independently against the function's known input provenance) but was not actually *demonstrated* by the tool — the analysis was single-function-scoped and could not verify a parameter's own provenance across a call boundary.

We closed this by extending the tool with interprocedural resolution: for a target parameter, find every real call site in the module and evaluate the taint of the bound argument at that call site, using the calling function's own taint state up to that point, rather than assuming the parameter is safe. Two fixes were necessary to make this work on a second, independently implemented codebase (PyBullet-based, not MuJoCo-based): the physical-actuation entry-point check, previously hardcoded to `env.step`, had to be generalized to a configurable set of method names, since this codebase's actual execution calls are named differently; and the calling function itself needed its own commit marker, since — consistent with the design already established — without one the tagger correctly flags nothing as tainted rather than guessing. This generalizes the earlier finding: interprocedural resolution does not shrink the marker requirement to one marker per codebase, it requires one marker per function with a genuine execution boundary, applied transitively up the call chain. With both fixes in place, resolving `_emit_lggsn_candidates`'s real call site correctly and automatically recovered every field's true provenance — including confirming that its `success`-derived `label` field is execution-derived (correct, since it is the training label, exempt under the Corollary) while its pose-derived features are genuinely pre-execution and safe for live selection. The tool no longer merely reproduces a fact established by separate manual tracing; it derives it.

### IV-E. An independently discovered infrastructure bug, and a stronger re-verification

While instrumenting a new admissible feature to test directly (whether the original, pre-commit candidate orientation — distinct from the disqualified execution-derived value in §IV-D — carries real signal), we found a second, unrelated bug: the Piper simulation's object-placement sampler was constructed without an explicit random-number generator, silently falling back to an OS-entropy-seeded default completely independent of the numpy legacy seed this project's every prior experiment relied on for reproducibility. Concretely, a scene's ten pooled candidates were never actually facing the same object placement, despite the code's own documentation asserting they were — confirmed empirically by re-running an unmodified data-collection script on an identical scene twice and observing three different outcome patterns across three runs, and independently by finding 7–9cm of spawn-position spread within already-collected data that should have been constant. Fixed by explicitly deriving a seeded generator from the legacy random state at scene-construction time, verified by confirming bit-identical object placement across repeated runs of the same seed.

This bug does not affect §IV-A through §IV-D: the causal-validity criterion and every provenance verdict in this paper come from tracing code, not from statistical properties of collected data. It does affect any specific accuracy number computed on data grouped by an assumption of shared placement. We re-collected the 25-scene Piper dataset under the fix and re-ran the corrected two-feature evaluation: the qualitative finding is unchanged — zero-shot, pooled-`none`, pooled-`additive`, and pooled-`interaction` remain exactly identical (0.1036, diff=0.0000) — confirming the paper's core claim is not an artifact of the placement bug.

We then tested the untested admissible feature this section had flagged as future work. A naive pooled check looked dramatic: candidate orientation correlates with success at r=−0.55 (p<0.0001, n=250) — far stronger than any other signal in this investigation. Decomposing it, following this paper's own stated discipline rather than reporting the encouraging number, revealed why: mean orientation and success rate correlate strongly *between* scenes (r=−0.71, p=0.0001 across 25 scenes) because different object placements are both associated with systematically different orientations and, for unrelated geometric reasons, different difficulty — but the *within*-scene correlation, the only one that could actually inform a live selection decision among candidates facing the same already-placed object, is indistinguishable from zero (mean +0.03 across 10 mixed-label scenes, no individual scene reaching significance). A feature that appears, by a wide margin, to be the strongest predictor found in this entire investigation carries zero real selection-relevant signal once correctly decomposed — a sharper demonstration of this paper's thesis than the original finding it was built to test.

---

## V. Case Study: A Silent Gripper-Controller Scaling Bug

While investigating why grasp success generalized poorly across object categories on the Piper platform, we traced the failure one level below the usual diagnostic stopping point — not "where does the arm end up," but "what does the gripper actually command at the actuator level" — and found that RoboSuite's `SimpleGripController` was silently re-scaling an already-real-units gripper action a second time. `PiperGripper.format_action` computes and returns an absolute, real-units joint-position target directly (a porting artifact from a different upstream convention), but the composite controller's default `use_action_scaling=True` applies its own normalized-to-real-units rescaling on top of it, unconditionally. At a verified, fully converged "fully open" command, the actual commanded actuator range spanned roughly one tenth of a millimeter, against a documented 76–100 mm. A first fix attempt — supplying explicit `input_min`/`input_max` bounds — had no effect, traced to RoboSuite's controller-assembly code discarding those keys during config rebuild; the actual fix was a single boolean, `use_action_scaling: False`, letting the already-correct action pass through unmodified.

A paired n=20-per-object pilot validated the fix: Cracker 0%→45%, Mustard 0%→65%, Pear unaffected at 65% (consistent with its prior 70–90% range within pilot-scale noise). A final confirmatory run at paper scale (n=40–60 per object, on a clean, previously unused trial range) reproduced Cracker (50%, n=40) and Mustard (70%, n=40) cleanly, but Pear did not replicate at its original figure — three independent 20-trial batches gave 40%, 40%, and 50% (pooled 43.3%, n=60), consistently below the original single-batch 65% (Fisher's exact p=0.12). No code drift was found that would explain a genuine regression; we report the larger, replicated figure (43.3%) as the confirmed rate rather than the flattering first number, consistent with this paper's stated discipline.

---

## VI. Toward Real-Hardware Deployment

Following our project's scope decision to include real-hardware capability rather than remain simulation-only, we built `PiperRealBackend`, an interface skeleton against the documented `piper_sdk` API, written while the physical arm was not connected to the development machine. Every hardware-touching method (`get_joint_positions`, `get_gripper_opening`, `move_joints`, `set_gripper`) raises an explicit error with a `VERIFY` marker rather than guessing an unconfirmed unit or scale convention, following the same relative-delta-clamped, replay-not-live-IK design pattern used by this project's validated SO-ARM101 hardware track. We present this as an architecture and an itemized pre-deployment safety checklist, not as a hardware result — physical validation is explicitly future work, gated on the checklist items stated in the design document.

---

## VII. What Doesn't Work: An Honest Negative-Results Ledger

Table I summarizes eleven mechanisms tested and closed over the course of this investigation, each evaluated with a paired-trial design and McNemar's exact test (or the noted equivalent). We include this not as padding but because four of these rows — independent, mechanistically distinct interventions on the same diagnosed execution-time failure — together constitute a complete, well-instrumented negative result on the specific open problem (sustained grazing contact under sub-millimeter miscentering during descent) this project's diagnostics repeatedly converge on: a binary contact-triggered hard stop (worse, 40% vs. 70%, premature triggering on incidental early contact); a sustained-contact variant requiring five consecutive contact-positive steps (a first batch appeared promising but did not replicate on a second, combined p=1.0000); a continuous force-proportional admittance damping (worse in two independent calibrations, with a stated mechanistic explanation — slowing down near the object extends the exposure window for an open gripper to push it, the opposite of contact-coordination mechanisms that only engage after fingers have already formed a grip); and pre-narrowing the gripper before descent to reduce sweep cross-section (worse, 20% vs. 50%). We state plainly that this specific failure mode is diagnosed, not solved, and requires either physical tactile sensing or a fundamentally different mechanism than the four tested here.

**TABLE I: Ruled-out mechanisms.** Every result is from a paired-trial design with McNemar's exact test unless noted. Full mechanistic explanations for each row are given in Appendix A.

| # | Mechanism | Result |
|---|---|---|
| 1 | OT-coupled conditional flow matching for candidate generation | Pooled −10.0pp vs. random-sampling baseline (p=0.0025); negative on all 7 objects tested |
| 2 | Condition-aware OT coupling (C²OT) fix for #1 | Partial, inconsistent mitigation; no improvement on the hardest object |
| 3 | Real-time in-the-loop world-model action correction | Net negative across 3 independent pilots (−9.3, −18.7, −13.3 pp) |
| 4 | Training-free consensus (median-pose) candidate selection | Effective on SO-ARM101 (+62pp, +30pp on two objects, Fisher's exact significant); did not replicate cross-embodiment under two independently designed noise models — re-verified under the §IV-E RNG fix (n=10/object), null confirmed on all three objects, not an RNG-bug artifact |
| 5 | AR-mediated active-calibration human-in-the-loop sampling | Active sampling significantly worse than passive logging (p=0.0048); calibration-aware refinement null (p=0.32–0.73) |
| 6 | Cross-embodiment candidate reranking (causally invalid features) | Apparent effect (p<0.0001 ×5) reduced to exact null (p=1.0000) once corrected — subject of §IV |
| 7 | Binary contact-triggered hard stop during descent | Re-verified†: null (65% vs. 65%, p=1.0000) |
| 8 | Sustained-contact stop (5 consecutive contact-positive steps) | Re-verified†: null both batches (p=1.0000 each) |
| 9 | Force-proportional admittance damping during descent | Re-verified†: null, 0 discordant pairs |
| 10 | Gripper pre-narrowing before descent | Re-verified†: worse, 75% vs. 15%, **p=0.0005** |
| 11 | World-model-driven sim-to-real transfer with scarce real data | Not piloted — closed at feasibility stage; both working prior-art mechanisms require infrastructure (thousands of real demos, or a video-diffusion-scale backbone) this project lacks |

†Rows 7–10 were originally reported as four independently negative mechanisms, before this paper's own §IV-E RNG-bug fix. Re-verified 2026-07-17 under corrected pairing: **three of the four (hard stop, sustained-contact stop, admittance damping) have no measurable effect** — their original "made things worse" findings were very likely themselves artifacts of the same RNG bug, which added spurious between-condition variance that looked like a real effect (row 9's re-verification is especially stark: 75% vs. 75% with *zero* discordant pairs across n=20 — the binary outcome matched on every single trial). **Only row 10 (pre-narrowing gripper clearance before descent) has a genuine, replicated effect**, and it strengthened substantially (p=0.0005 vs. the original p=0.11, not significant). The corrected reading: contact-*response* interventions (react once contact is detected mid-descent) are inert for this failure mode, most likely because they rarely trigger in a way that matters; the one intervention that changes baseline geometry *before* any contact occurs is the one with a real cost. This is narrower and more mechanistically precise than "everything we tried backfired," and it is the reading we stand behind. The underlying failure mode remains diagnosed, not solved, requiring either physical tactile sensing or a mechanism fundamentally different from the four tested here.

---

## VIII. Conclusion

We set out to improve cross-embodiment grasp reliability and found, instead, that our own strongest apparent result in that direction was an artifact of a silent, well-known-in-the-abstract but rarely-formalized class of evaluation error. Rather than treat that as a dead end, we formalized the constraint it violated, built and validated both a manual and an automated tool to check for it, and found — twice — that even careful, motivated verification still misses real instances of the problem it is looking for, until the process is made mechanical. Alongside this, we report a genuine, validated fix to a previously undiagnosed execution bug that unlocked most of a robot platform's latent grasp reliability, an honest account of eleven mechanisms that did not work for the problem that remains, and an architecture for the real-hardware phase this project has not yet reached. We believe the discipline demonstrated across all of these — verify against ground truth, report the number that survives replication rather than the one that looked best first, and say plainly what remains unsolved — is a more durable contribution than any single mechanism we tested.

As a next step, motivated by independent evidence [Joyce et al., IROS 2025] that synthetic Gaussian perturbation does not reproduce real pose-estimator failure modes, we are instrumenting a real RGB-D sensor to empirically characterize pose-estimation noise structure (per-axis variance, Gaussianity, position-orientation correlation) against the isotropic-Gaussian assumption our own candidate-selection experiments relied on, and — contingent on that measurement actually showing a structural difference, not assumed in advance — to test whether a selection rule informed by the measured structure changes the cross-embodiment generalization result in Appendix A row 4. Any such feature remains checkable against §IV's criterion, since it would be computed purely from the candidate pool's own poses and the measured noise covariance. This work has not yet started data collection and is not included in the results reported above.

---

## Appendix A: Full Ruled-Out-Mechanisms Ledger

Every entry was tested with a paired-trial design and McNemar's exact test (or the noted equivalent).

| # | Method | Mechanism | Result | Why it failed (mechanistic, where known) |
|---|--------|-----------|--------|-------------------------------------------|
| 1 | OT-CFM candidate generation | Minibatch-OT-coupled conditional flow matching for grasp candidate generation, replacing random CoM sampling | Pooled −10.0pp vs. baseline, p=0.0025; negative on all 7 objects | Minibatch OT coupling ignores conditioning (object identity) — training/inference distribution mismatch, consistent with reported failure modes of unconditional OT coupling in conditional flow-matching settings |
| 2 | C²OT (condition-aware OT fix) | Condition-aware cost-matrix reweighting, applied to fix #1 | Partial, inconsistent mitigation; hardest object (Pear) showed no improvement at all | The literature fix, validated only on generation-quality metrics, does not fully transfer to physical grasp-execution success |
| 3 | MPC-style real-time world-model correction | Small-range action search/correction just before gripper close, using a learned state-transition model | Net negative across 3 independent physical pilots (−9.3pp / −18.7pp / −13.3pp) | Real-time in-the-loop correction search consistently made things worse, not better |
| 4 | Ensemble + consensus candidate selection (median-pose, not min-IK-error) | Sample N candidates, pick the one nearest the pool median instead of the one with lowest IK error | Worked on SO-ARM101 (+62pp, +30pp on two objects, Fisher's exact significant) but did not replicate on the Piper platform under two independently designed noise models | Training-free geometric heuristic; a genuine cross-embodiment generalization failure, not a bug |
| 5 | AR-mediated active calibration (passthrough-AR human-in-the-loop) | World-model rollout prediction shown to a human; human confirms/corrects before execution; active uncertainty-driven sampling of human judgment | Two independent pilots: active sampling significantly worse than passive (p=0.0048); a calibration-aware refinement showed a null result (p=0.32–0.73) | Active solicitation of human judgment provided no measurable benefit over passively logging execution outcomes |
| 6 | Cross-embodiment grasp candidate reranking (§IV) | Train a reranker on pooled data from both platforms to select among not-yet-executed candidates | Apparent effect (p<0.0001, ×5) traced to causally invalid features; corrected once to `[z, yaw, H]` (null, p=1.0000), corrected again after `auto_tagger.py` caught a second leak in `yaw` itself, to `[z, H]` (null still holds, p=1.0000; reported accuracy changed 0.8236→0.1327) | No causally valid pre-execution signal available in this feature set predicts the target object's grasp success; the one genuinely valid feature tested (an isolated kinematic IK residual) showed only 0.06 correlation with outcome |
| 7 | Compliant/contact-aware descend v1 (binary hard-stop on any contact) | Stop descend motion the instant any object contact is detected | *Original* (RNG-bug-affected): worse, 40% vs. 70%. **Re-verified**: null, 65% vs. 65%, p=1.0000, 2 discordant pairs | Original finding does not replicate under valid pairing — no measurable effect |
| 8 | Compliant/contact-aware descend v2 (sustained contact, 5-step threshold) | Same as v1 but require 5 consecutive contact-positive steps before stopping | *Original*: promising batch 1 (75%/55%), opposite-direction batch 2 (65%/50%), combined p=1.0000. **Re-verified**: both batches agree, 75% vs. 70%, p=1.0000 each | Original instability was itself likely an RNG-bug artifact; valid pairing gives a stable, consistent null |
| 9 | Force-aware admittance descend v3 (continuous damping proportional to contact force) | Progress increment damped as `1/(1+gain·max(0,force−threshold))` | *Original*: worse in 2 calibrations (55%/75%, 50%/75%). **Re-verified**: 75% vs. 75%, *zero* discordant pairs across n=20 | Original mechanistic story (exposure-window extension) does not hold under valid pairing — no measurable effect, not a harmful one |
| 10 | Pre-narrow-before-descend v4 (partially close gripper before descending) | Narrow gripper opening before descend, hold narrow width through descend | *Original*: worse, 20% vs. 50%, p=0.11 (n.s.). **Re-verified**: worse, 15% vs. 75%, **p=0.0005** | The only one of the four with a genuine, replicated, strengthened effect — permanently reducing pre-descent clearance measurably hurts |
| 11 | World-model-driven sim-to-real transfer with scarce real data | Train a policy in simulation, optionally using a learned world model as a data source, deploy with little or no real-world data | Not piloted — closed at the feasibility-check stage | Both working prior-art mechanisms require infrastructure this project lacks: on the order of thousands of real demonstrations to amplify, or a video-diffusion-scale backbone and a synthetic-data-generation pipeline that does not exist here |

---

## Notes for revision (not part of the submission)

- [x] Abstract and Introduction tightened.
- [x] Section VII's condensed table pulled in (11 rows, one line each).
- [x] Appendix A added (full mechanistic-explanation table); row 6 updated to reflect the §IV-D `grasp_yaw` re-correction so it's consistent with the main text.
- [x] Diagnostic experiment: verified (not assumed) why the corrected `[z, H]` Stage 2 accuracy is 0.1327 — `H` confirmed exactly constant (σ=0, n=250) and `z` confirmed near-constant per scene (14/25 scenes, spread <2e-4) directly against the raw pairwise-collection data. Explanation tightened everywhere it appears (`paper_tro.tex`, `AUTO_TAGGER_ALGORITHM.md`, `IDEA_REPORT.md`, `retrospective_audit.py`'s own output text).
- [x] Bibliography pass done for all 6 previously-flagged citations (`graspgen2025`, `graspgenx2026`, `fpl2026`, `feasibility2025`, `offlinerl_leakage2026`, `coorgrasp2026`) — full author lists confirmed via each paper's arXiv page, `%% VERIFY` markers removed, `paper_tro.tex` recompiles clean (6 pages, 0 undefined refs/citations).
- [ ] LGGSN's RA-L citation placeholder in §I/§III still needs the actual paper's final citation once available (currently under review, submitted 2026-07-11 — do not cite as published until accepted). Not actionable until that review resolves.
- [x] **Decided: keep results distributed, do not consolidate into one section.** This is not a single propose-method/evaluate-method paper — it's three largely independent findings (the causal-validity audit, the gripper-controller bug, the negative-results ledger), each with its own mechanism and its own evidence. A reader following §IV's audit story needs its numbers right there, not three sections later next to an unrelated gripper-bug result; the same holds for §V and §VII. Consolidating would force constant back-referencing across unrelated findings for no real gain in rigor. The distributed structure is the more readable choice for this paper's actual shape, not a shortcut.
- [ ] Optional stretch, not required for submission: extend `auto_tagger.py` to interprocedural taint tracking (§IV-D already states the current single-function scope as an honest limitation, not a hidden gap).
- [ ] Once Appendix A exists and citations are real, ready for `/paper-compile` against a proper LaTeX skeleton (T-RO template, not yet created).
