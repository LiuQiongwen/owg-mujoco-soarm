"""Subprocess adapters for the Codex CLI (read-only planner/reviewer) and
the Claude Code CLI (isolated-worktree executor), plus deterministic mock
implementations used by default and in all automated tests."""
